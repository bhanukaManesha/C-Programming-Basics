# include <stdio.h>
# include <stdbool.h>

int main(){
    int bhanuka = 30;
    
    // Using the classical definition
    _Bool myOldBool = true;

    // Using the standard boolen library
    bool myBool = true;

    return 0;
}